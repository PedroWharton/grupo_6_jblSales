<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <main>
        <h1 style="text-align:center;">Registrate</h1>
        <form action="/user/register" method="POST" enctype="multipart/form-data" id="form">    
                <div class="register-form">
                    <ul class="errors"></ul>
                    <div>
                        <label for="username"><b>Nombre de Usuario</b></label><br>
                        <input type="text" placeholder="Nombre de Usuario" name="username" id="username"
                               class="form-control <%= locals.errors && errors.username ? 'is-invalid' : locals.errors ? 'is-valid' : null %>"
                               value="<%= locals.oldData ? oldData.username : null %>"  ><br>
                        <% if(locals.errors && errors.username) { %>
                            <div class="text-danger"><%= errors.username.msg %></div>
                        <% } %> 
                    </div>
                    <div>
                        <label for="email"><b>Email</b></label><br>
                        <input type="text" placeholder="Email" name="email" id="email" 
                               class="form-control <%= locals.errors && errors.email ? 'is-invalid' : locals.errors ? 'is-valid' : null %>" 
                               value="<%= locals.oldData ? oldData.email : null %>"><br>
                        <% if(locals.errors && errors.email) { %>
                            <div class="text-danger"><%= errors.email.msg %></div>
                        <% } %>
                    </div>
                    <div>
                        <label for="avatar"><b>Foto De Perfil</b></label><br>
                        <input type="file" placeholder="avatar" name="avatar" id="avatar" 
                               class="form-control <%= locals.errors && errors.avatar ? 'is-invalid' : locals.errors ? 'is-valid' : null %>" 
                               value="<%= locals.oldData ? oldData.avatar : null %>"><br>
                        <% if(locals.errors && errors.avatar) { %>
                            <div class="text-danger"><%= errors.avatar.msg %></div>
                        <% } %>
                    </div>
                    <div>
                        <label for="password"><b>Contraseña</b></label><br>
                        <input type="password" placeholder="Contraseña" name="password" id="password"
                               class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %>" ><br>
                        <% if(locals.errors && errors.password) { %>
                            <div class="text-danger"><%= errors.password.msg %></div>
                        <% } %>
                    </div>
                    <div>
                        <label for="pswRepeat"><b>Repita Contraseña</b></label><br>
                        <input type="password" placeholder="Repita contraseña" name="pswRepeat" id="pswRepeat"
                               class="form-control <%= locals.errors && errors.pswRepeat ? 'is-invalid' : null %>" ><br>
                        <% if(locals.errors && errors.pswRepeat) { %>
                            <div class="text-danger"><%= errors.pswRepeat.msg %></div>
                        <% } %>
                        <div id="checkbox">
                            <label for="remember">Recordame</label>
                            <input type="checkbox" name="remember" class="form-check-label" id="checkbox-input">
                        </div>
                        <button type="submit" class="register-button">Registrate</button>
                        <p>Ya tenes una cuenta? <a href="/user/login">Login</a></p>
                    </div>
                
                </div>
               
        </form>
    </main>
    <%- include('../partials/footer') %>
    <script src="/js/registerValidationFront.js"></script>
</body>
</html>    
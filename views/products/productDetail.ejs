<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
    <%- include('../partials/header') %>
    <main>
        <a href="/">&lt; Volver a la pagina anterior</a>
        <article class="main-product-detail">
            <form action="/products/productDetail/<%= product.id %>" method="POST">
                <div class="main-product-detail-div">
                    <img src="/images/<%= product.img %>" alt="" class="img-product-detail">
                <div class="desktop-description-div">
                    <h4 class="nombre-del-producto"><%= product.name %></h4>
                    <p class="descripcion-producto"><%= product.description %></p>
                    <p class="precio-producto">$<%= product.price %></p>
                    <div class="cantidad-select">
                        <p>Cantidad</p>
                        <select name="cantidad" id="">
                            <option value="1">1</option>
                            <option value="2">2</option>
                            <option value="3">3</option>
                            <option value="4">4</option>
                            <option value="5">5</option>
                        </select>
                    </div>
                    <div class="product-detail-buttons">
                        <button type="submit" class="comprar-ahora">Comprar Ahora</button>
                        <button type="submit" class="agregar-carrito">Agregar al Carrito</button>
                    </div>
                </div>
            </form>
            </div>
            <div class="caracteristicas">
                <h4>Caracteristicas</h4>
                <p><%= product.caracteristics %></p>

            </div>
            <div class="descripcion">
                <h4>Descripcion</h4>
                <p><%= product.description %></p>

            </div>
        </article>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>    